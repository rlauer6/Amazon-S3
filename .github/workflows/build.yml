name: Amazon-S3
on:
  push:
    branches: [ master, dev ]
jobs:
  Amazon-S3:
    runs-on: ubuntu-latest
    container:
      image: debian:trixie
    steps:
      - uses: actions/checkout@v7

      - name: build
        run: ./builder https://github.com/rlauer6/Amazon-S3.git
